// Import the Home component from the "pages" directory
import Home from "./pages/Home";

// Define the root component of the React application
function App() {
  // Render the Home component as the main content of the application
  return (
    <Home />
  );
}

// Export the App component as the default export
export default App;

